//ShowParamChange(type, value, notice)

var type, value, notice;

type = argument0;
value = argument1;
notice = argument2;

switch(type)
{
    case PARAM_CHANGE_TYPE_JUMP:
        {
            switch(value)
            {
                case PLAYER_JUMP_RESTRICT_INFINITE:
                    {

                    }
                    break;
                default:
                    break;
            }
        }
        break;
    default:
        break;
}
