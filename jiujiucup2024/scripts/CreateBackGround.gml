//CreateBackGround(color, breath time, grid color, grid size)
/*
var color, time, gridColor, gridSize;

color = argument0;
time = argument1;
gridColor = argument2;
gridSize = argument3;

var inst;

inst = instance_create(0, 0, bgBreath);
inst.color = color;
inst.breathTime = time;
