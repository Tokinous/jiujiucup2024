/*
var dir, i, n;

n = 48;
/*
i = 0;
dir = point_direction(0, view_yview[0] + 608, 400, player.y);

repeat(n)
{
    createCircle(0, view_yview[0] + 608, 24 + i*24, argument0, argument1, 1, 0, dir);
    i += 1;
}

i = 0;
dir = point_direction(800, view_yview[0] + 608, 400, player.y);

i = 0;
//dir = point_direction(400, view_yview[0], player.x, player.y);

dir = random(360);

repeat(n)
{
    createCircle(400, view_yview[0], 24 + i*24, argument0, argument1, 1, 0, dir);
    i += 1;
}
