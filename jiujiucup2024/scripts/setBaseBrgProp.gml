//img_index, scale
//use -1 to keep no change


    // current index for base barrage
if(-1 != argument0)
    global.cur_image_index = argument0;
    // current sprite for base barrage
if(-1 != argument1)
    global.cur_image_scale = argument1;
